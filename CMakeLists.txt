message("")
message("**** configuring KratosFourCApplication ****")

if(${CMAKE_SYSTEM_NAME} MATCHES "Android")
    message("KratosFourCApplication is compiled on Android")
    include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists2.txt)
else()
    if((${PYTHON_VERSION_MAJOR} MATCHES "2") OR (${FOUR_C_APPLICATION_USE_BOOST_PYTHON} MATCHES TRUE))
        if((${PYTHON_VERSION_MAJOR} MATCHES "3") AND (${FOUR_C_APPLICATION_USE_BOOST_PYTHON} MATCHES TRUE))
            message("KratosFourCApplication is compiled with Python 3 (Boost.Python)")
        else()
            message("KratosFourCApplication is compiled with Python 2")
        endif()
        include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists2.txt)
    elseif(${PYTHON_VERSION_MAJOR} MATCHES "3")
        message("KratosFourCApplication is compiled with Python 3")
        include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists3.txt)
    else()
        message("KratosFourCApplication does not detect the Python version")
    endif()
endif()

message("**** finish configuring KratosFourCApplication ****")
message("")
